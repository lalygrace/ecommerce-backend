# Example environment variables for local development
# Copy to `.env` and fill values before running the app
NODE_ENV=development
# Run backend on a different port than Next.js to avoid proxy loops
PORT=4000
# Better Auth base URL should point to the frontend origin (Next.js dev server)
BETTER_AUTH_URL=http://localhost:3000
# Optional app metadata used in emails
APP_NAME=Ecommerce
SUPPORT_EMAIL=support@example.com
# Database connection - required (Postgres/Mongo depending on your setup). The app expects MONGO_URI or DATABASE_URL
DATABASE_URL=postgresql://user:password@localhost:5432/ecommerce_db
# Optional Google OAuth (only used if set)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Example .env for local development with PostgreSQL
# Copy this to `.env` and update the values before running migrations.

# Postgres URL format: postgres://USER:PASSWORD@HOST:PORT/DATABASE
# Example (local):
DATABASE_URL="postgres://postgres:postgres@localhost:5432/ecommerce_dev"

# If you prefer to use a hosted Postgres service, replace the URL above accordingly.

# SMTP configuration (for sending verification emails)
# If these are not set, emails will be logged to the console in development
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Ecommerce <no-reply@example.com>"
 